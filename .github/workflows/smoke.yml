name: Smoke

on:
    push:
    workflow_dispatch:

jobs:
    smoke:
        runs-on: ubuntu-latest
        steps:
            - name: Confirm GitHub Actions can run
              run: echo "Actions works"
